#ifndef SPLITTER_H
#define SPLITTER_H

/*
    Function that splits out the digit
    at position index of number toSplit.
    
    index start from zero, from units, in
    ascending order
*/
int splitter(int toSplit, int index);


#endif